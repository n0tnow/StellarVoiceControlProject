{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the Polaris windows: the main notch overlay and the A6 typed-prompt panel. Core IPC, the event stream and window control; plugin permissions (global shortcut, later) are added per step.",
  "windows": ["main", "prompt"],
  "permissions": ["core:default"]
}
