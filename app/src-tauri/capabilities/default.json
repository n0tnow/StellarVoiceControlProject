{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the Polaris window: the notch overlay, which now hosts the folded A6 typed prompt as a shell state. Core IPC, the event stream and window control; plugin permissions (global shortcut, later) are added per step.",
  "windows": ["main"],
  "permissions": ["core:default"]
}