# backlog.md — Master Index of Unfinished Work

> This file is the **master index of all unfinished / handed-off work.**
> Every worker and sub-agent writes unfinished work as a detailed report in `backlog/<task-name>.md`
> and adds a single-line entry here. **Goal: never lose track of any task.**
> All entries and reports are written in **English**.
>
> Index row format:
> `| Date | Task | Worker/Agent | Status | Report | Priority |`

## Open Tasks

> Most rows below were merged into `integration/wallet-login` and are pending final review/merge
> to `main`; the genuinely open items are listed in `sprints.md` **Milestone 5**. Rows are not
> rewritten (history).

| Date | Task | Worker/Agent | Status | Report | Priority |
|---|---|---|---|---|---|
| 2026-09-20 | **UI-TASKS-RULES-PRO — Tasks/Rules pages: fix slow open/load + professional redesign**: root cause was per-visit unmount → a long serial IPC+RPC read with no cache/skeleton (`useTasksData`, `useRulesEditor`); added module-level stale-while-revalidate caches + in-flight dedupe, parallelised the Rules read and cut the duplicated `executor_status` probe, skeleton/empty/error states, memoized rows; redesigned both pages with a header, status dot, advanced-behind-a-text-button and max-two-visible-inputs; `npm run check` clean, app **485/485**, stellar **112/112**, build green; real WKWebView/Touch ID need a human (no visual harness) | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/ui-tasks-rules-pro.md` | P1 |
| 2026-09-20 | **NOTCH-THEME — wallet-unlock + approval cards on the menu-page theme**: both cards rebuilt from the shared page materials (`rule-card`, `wallet-key` rows, `rule-head`/`rule-body`, new `Button` `notch`/`notchOutline` variants) instead of navy borders + sky-blue + a magenta ring; the approval overlay now covers the page column only (nav + Close stay visible, no blur) and `.notch-panel { overflow: hidden }` restores the shell's rounded bottom corners; progress bar reuses `.wallet-budget-bar` (accent); check clean, app 475/0, build green, screenshots in `backlog/assets/notch-theme/` | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/notch-theme-wallet-approval.md` | P1 |
| 2026-09-20 | **UI-SFX — minimal UI sound layer**: `uisfx@0.4.0` (WebAudio-synthesized, no assets/network) wrapped in `app/src/lib/sfx.ts` (`playSfx`/`unlockSfx`/`setSfxEnabled`/`isSfxEnabled`/`useSfx`, lazy + fail-silent, `localStorage`-persisted enabled pref, reduced-motion-aware default); wired into `ShellSurface` open/close, `MoreMenu` hover/select, `Button` press, `ApprovalPanel` success/error; tsc clean, 392/392 tests pass, `scripts/check.sh` green | worker (Claude Sonnet 5) | open (review) | `backlog/ui-sfx.md` | P2 |
| 2026-09-20 | **ONBOARDING-UI — first-run window**: six-page centred window (welcome / permissions / push-to-talk / typed prompt / notch / ready) on the notch's own 470ms curve, pure step machine + gesture probes (35 tests), BlobatarFace reused for the listening pose; **page 5 illustrates rather than gates — `notch_hover` cannot reach a focused window, evidence in the report §1** | Claude Opus 5 (UI worker) | open (review) | `backlog/onboarding-ui.md` | P0 |
| 2026-09-20 | **W18 — "Add asset" (trustline) so faucet USDC / anchor SRT can be received**: new unsigned `changeTrust` builder (`stellar/src/payments/trustline.ts`, pinned USDC/SRT issuers, SAC test) + `app/src/lib/trustline.ts` wired through the existing `runTx` Touch-ID pipeline; Wallet "Add asset" list with `Added` state, asset chip and USDC faucet hint + allow-listed `faucet.circle.com`; P2P missing-trustline sentence gets an "Add USDC" action; check clean, app **416/0**, payments **135/0** (9 new), cargo 3/3 + clippy | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w18-trustline.md` | P0 |
| 2026-09-20 | **W17b — P2P "Create offer" HostError #13 fixed**: SAC/escrow error codes mapped to plain sentences (`p2pErrorMessage`), fail-closed seller balance/trustline preflight from Horizon before the approval card, held-asset picker + accurate "Amount (<ASSET>)" labels; live testnet simulation confirmed #13 = missing trustline; stellar/app `check` clean, stellar p2p **25/0**, app **415/0** | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w17b-p2p.md` | P0 |
| 2026-09-20 | **W17a — Trade deposit/withdraw on the SDF test anchor + safe fallback**: session now derives from the ACTIVE scenario (SDF USD/SRT vs TR TRY/USDC), so SEP-38 no longer mixes TRY with the SDF anchor; new pure `selection.ts` preflights approved anchors (SEP-1 + SEP-6 info, ~6 s) and picks the first healthy one before any debit/payment (no fallback after value moved), one plain line + Details toggle when none is healthy; "via …" caption, scenario labels; check clean, stellar anchor 219/0 (8 new), app 412/0, live SDF preflight-only smoke ok | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w17a-anchor.md` | P0 |
| 2026-09-20 | **W16 — every explorer link opens in the default browser**: `ExplorerLink` `iconOnly` variant + links wired through `open_external` at the Wallet address/account rows, History drawer, Trade results, Rules/Tasks results and the approval overlay "Sent ✓" (`tx_submitted`); replaced the dead `<a target="_blank">` links; check clean, app 412/0 | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w16-links.md` | P1 |
| 2026-09-20 | **W15h — remove the panel-window infrastructure (the notch is the only surface)**: deleted `app/src/panels/**` + `lib/panels.ts`, Rust `panels.rs`/`open_panel`/`panel-*` capability + the panel close/resign-active handler, and panel hash routing in `main.tsx`; moved still-used `shortAddress`→`lib/address.ts`, the suggestions model→`lib/suggestionsModel.ts`, `quitPolaris`→`lib/app.ts`; docs `docs/ui-panels.md`→`docs/notch-ui.md`; check/app 412/cargo 364/clippy/build green | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w15h-cleanup.md` | P0 |
| 2026-09-20 | **W15c — Trade page (anchor deposit/withdraw + P2P) inside the notch**: replaced the `TradePage` stub with a 3-segment page (Deposit·Withdraw·P2P); deposit/withdraw reuse BANK-SIM (`bankDeposit`/`bankWithdraw`) with one amount field (default 10) + one primary button and a 3-row step list (Bank debited→Anchor received→Wallet credited, reversed for withdraw), default `sdf-test` scenario, bank+wallet balance line; P2P lists open offers + Accept and a "Sell" form → `create_offer` reusing `lib/p2p*.ts`; locked → `LoginGate`; `app/src/notch/trade/**` + `TradePage.tsx`; check clean, app **444/0** (10 new), build green; live SDF/Touch ID/P2P need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w15c-trade.md` | P0 |
| 2026-09-20 | **W15d — Settings page**: real `SettingsPage` (Session auto-lock/logout, Status Voice/Network/Contracts/Touch ID rows, 3-line Privacy + read-only SPP status, Diagnostics "Run all checks" over the FeatureCheck registry, Quit Polaris); pure `notch/settings/statusModel.ts` + 9 tests; check/build green, app 443/0 | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w15d-settings.md` | P1 |
| 2026-09-20 | **W15a — shell: closable panel / no popups / voice nav in the notch**: removed `shouldPinWallet` (panel is always closable; Wallet auto-opens once at launch; logout/auto-lock collapses it via `didSessionLock`), deleted the "⋯" `MoreMenu` + `MORE_MENU`, mapped every `NavigationTarget` to a notch page (security/schedules/suggestions/anchor/p2p/privacy/debug folded in; `applyNavigation` now a no-op), compacted `.panel-nav`; check clean, app 432/0; live notch hover/Close/Escape need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w15a-shell.md` | P0 |
| 2026-09-20 | **W15e — Rules & Tasks pages: simple, voice-first, complete**: notch Rules page now a live plain-language summary + single "Ask me only above" control + Save through the auto-pay batch flow (executor auto-setup, Advanced reveals per-payment/per-day/contacts + on-chain read-back); Tasks page real schedules + Cancel + a text "New schedule" reveal (To/Amount/Every/First run → approval pipeline), demo rows only outside Tauri; `normalizeForm`/`ruleSummary` + new `notch/tasks` helpers, check clean, app 442/0 | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w15e-rules.md` | P1 |
| 2026-09-20 | **W15b — Wallet page radically simpler** (SHORT): in-app "Fund account" (Friendbot fetch; CORS verified), big XLM balance hero + asset chips, address copy/inline QR, two-input Send (contact suggestions, asset switcher only when >1), compact Contacts with "Add contact" + cross-view `polaris:contacts-changed`, Accounts switcher/Add/Manage (auto-lock dropped, moves to Settings); app check clean, 435/0 tests; live notch + real Friendbot need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w15b-wallet.md` | P0 |
| 2026-09-20 | **W15f — "Ask Polaris" saves a contact by text/voice**: read-only `save_contact`/`list_contacts`/`delete_contact` (name uniqueness, secret-key + recovery-phrase refusal, missing-address ask), app store over the existing `contacts_*` commands + StrKey checksum, `polaris:contacts-changed` DOM event, prompt rules/examples, 10 eval cases; check clean, agent 231/0, app 440/0 | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w15f-contact.md` | P1 |
| 2026-09-20 | **W15g — approval card INSIDE the notch (no approval popup window)**: new `notch/approval/**` overlay (`usePendingApproval` + pure `overlayModel`, card/batch card/hash fingerprint) pins the notch `panel` and renders over the page body; `approver.ts` no longer calls `open_panel("approval")`; result dwell then collapse; check clean, app 446/0 | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w15g-approval.md` | P0 |
| 2026-09-20 | **DOCS-FINAL — bring README / sprints / backlog / demo docs in line with the current product** (SHORT): README status/setup; `sprints.md` Milestone 5 summary + superseded V/C/A rows; `docs/wallet-track.md` design record (session/autonomy/anchor); `docs/demo-script.md` + `docs/pitch.md` rewritten; `contracts/DEPLOYED.md` escrow-review limitations; `docs/reviews/` archive; branch `docs/final-sync` | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/docs-final-sync.md` | P1 |
| 2026-09-20 | **W14 — "Connect your existing wallet" as the primary first-run path**: ConnectScreen now leads with **"Connect existing wallet"** (import, secret mode default) over "Create new wallet" with new Freighter/Lobstr/xBull copy; ImportWallet relabelled to Connect ("Connect existing wallet" / "Connect this account?" / "Connect and store in Keychain"); shared `CONNECT_COPY` in `walletFlows.ts`; confirmed Rust `session::import` leaves the session unlocked so the user lands on the dashboard (no dead end); check clean, app 434/0 | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w14-connect-wallet.md` | P0 |
| 2026-09-20 | **MERGE-FINAL — merge `feat/w11a-executor-rust`, `feat/w11b-autopay-ui`, `chore/remove-freighter-bridge` into `integration/wallet-login`**: `lib.rs` union once (bank + executor + approval batch, no `bridge_*`), `approval.rs` doc / `RulesPage.tsx` / `package.json` / Cargo.lock resolved; check/app 416/agent 213/stellar 1044/build/cargo 364/clippy green, `git grep bridge_sign` empty in code/lockfiles, `e2e:autopay OK`; committed not pushed | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w12-remove-freighter.md` | P0 |
| 2026-09-20 | **W12 — remove the Freighter bridge**: deleted the Rust loopback server/launcher + `bridge_*` commands + `tiny_http`, the bridge page/`app/src/bridge`/Wallets Kit dep/fixture/docs, and the `POLARIS_SIGNER`/`bridge_sign` branches; kept `verify.rs`/`strkey.rs` + a new `bridge/outcome.rs`; check/app 396/agent 213/stellar 112/build/cargo 348/clippy green, no wallets-kit chunk in `dist` | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w12-remove-freighter.md` | P0 |
| 2026-09-20 | **MERGE-BANK — merge `feat/bank-sim-automation` into `integration/wallet-login`**: resolved `lib.rs` (both command lists + setup blocks once), `backlog.md`/`sprints.md` unions; deposit/withdraw route to `runBankIntent` behind the wallet session gate + Touch ID pipeline; SEP-10 challenge via `wallet_sign_challenge`, no Freighter in new paths; check clean, app 431/0, agent 213/0, stellar 970/0, build OK, cargo 386/0/5-ignored, clippy clean | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/bank-sim.md` | P0 |
| 2026-09-20 | **W11a — Rust executor key, safe `executor_sign_pay`, batch approval**: `wallet/executor.rs` (per-owner Ed25519 key, strict one-shape signer, health/commands) + `approval.rs` one-Touch-ID batch; `stellar-xdr` decode, hard cap, adversarial + fuzz tests; cargo 393/0/5-ignored + clippy, check/app 421/stellar 112 green | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w11a-executor-rust.md` | P0 |
| 2026-09-20 | **W11a-fix — apply the executor security review**: fee/resource-fee/5-min-time-bound/no-memo checks, 16 KiB input cap + bounded decode (depth 32), auth-tree equality, atomic `executor_create`, per-asset decimals allow-list, batch deny/lock emit every step hash, `wallet_remove` deletes the executor; cargo 372/0/5-ignored + clippy, check/app 416/stellar 112/agent 213, `e2e:autopay OK` | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w11a-executor-rust-review-fixes.md` | P0 |
| 2026-09-20 | **MERGE-HIST — merge `feat/history-ui-pro` into `integration/wallet-login`**: kept the pro History timeline as `HistoryBody` wrapped by `LoginGate`/`useWalletLocked` like Tasks/Rules (chain hook never mounts locked), kept both backlog rows; docs/auto-merge no drift; check/app 418/agent 213/stellar 112/build/cargo 375/clippy green | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/history-ui-pro.md` | P0 |
| 2026-09-20 | **NOTCH-CLIP — wallet gate panel was clipped**: the native click-through watchdog force-collapsed the pinned/interactive wallet login panel ~1.5 s after launch (cursor outside) while React kept `applied=panel`, so the full panel was painted into the collapsed window; added a `pin` shell source (attention voice still outranks; the pin returns after) + `shell_set_pinned`/runtime `pinned` watchdog exemption, `shellDiagnostics` + Debug state readout, `.panel-body` `min-height:0` scroll; app 407/0, cargo 377/0/5-ignored, check/build/clippy green | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/fix-notch-clip.md` | P0 |
| 2026-09-20 | **MERGE-WALLET2 — merge W13a session + W13b experience onto the security-fixed engine**: resolved `wallet/mod.rs` (kept `SignerChanged` + `Locked`), `interfaces`, `WalletPage`/`LegacyWalletView`, debug check; one `WalletSession` definition in `@polaris/interfaces`; fixed `walletSessionLive` to the `polaris-event` channel; E2E path read-confirmed; check/app 404/agent 190/stellar 112/build/cargo 375/clippy green; live Touch ID/auto-lock need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w13a-wallet-session.md` | P0 |
| 2026-09-20 | **W13b — professional wallet experience**: session-driven Wallet page (Connect/Unlock/Dashboard) with startup auto-open + pin on Wallet, UI gate on History/Tasks/Rules + "⋯", professional dashboard (network/Funded badges, full key + copy/explorer, dependency-free QR Receive, trustlines + native reserve, 10-op activity, account switcher, Send via `executeApprovedIntent`, Log out + auto-lock), session gate sentence; app 404/0, agent 190/0, check/build green; W13a Rust session commands not yet in branch (feature-detected, fakes only) | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w13b-wallet-experience.md` | P0 |
| 2026-09-20 | **VOICE-DIALOG — conversational memory + rules/sell/buy by voice**: in-memory last-≤4-exchanges + pending clarification (`dialog.ts`, wired in `loop.ts`/`agent.ts`), `set_approval_rule` proposal (`Intent.rule`, dollar asks USDC/XLM, not enabled in this build), `sell_asset`/`buy_asset` mapping to existing `withdraw`/`p2p_offer`/`deposit`/`p2p_accept` (missing route asks one question), prompt taxonomy + tr/en; agent 213, app 385, app build green, live `e2e:prompt` 90/90=100% (glm-5.3-flash; Anthropic key out of credit) | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/voice-dialog.md` | P1 |
| 2026-09-20 | **HISTORY-UI — pro History timeline**: `notch/history/` pure model (7-decimal bigint amounts, kind/status/route/approval), filters+search (`All/Sent/Received/Auto/P2P/Anchor/Voice`), sticky day groups, detail drawer, CSV copy; `useHistoryData` Horizon cursor paging (20/page, 200 cap) + offline; `turnLog` metadata fields; P2P/anchor best-effort rows; check/app 399 pass/build green, real notch needs a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/history-ui-pro.md` | P1 |
| 2026-09-20 | **ANCHOR-SDF — Stellar SDF test anchor as working scenario**: fixed SEP-10 window check (`maxTime-minTime`, not `maxTime-now`); `sdf-test` first-class with fake demo SEP-12 customer auto-fill (unknown fields → `KycRequiredError`); check green, stellar 1037 tests green, live check + manual deposit verified; e2e stops at SDF's USD/TRY SEP-38 mismatch + per-transaction identity KYC | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/anchor-sep10-validity.md` | P1 |
| 2026-09-20 | **BANK-SIM — simulated bank + automated on/off-ramp loop**: Rust demo ledger (`bank.rs`: atomic `bank.json`, TR mod-97 IBAN, integer money, idempotent credit/refund/settle) + commands/`bank_changed` event; pure `bankFlow.ts` automation (deposit reserve→refund on failure, withdraw credit-once, restart reconciliation) wired to the shell `AnchorSession` with `POLARIS_ANCHOR_HOME_DOMAIN` (default SDF `testanchor.stellar.org`); Anchor-panel bank card/forms/history; voice `deposit`/`withdraw` routed to `runBankIntent`; `bank` Debug check; cargo 328/0 + clippy clean, app 372/372 + check/build green; live Touch ID/Freighter + real Mac need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/bank-sim.md` | P0 |
| 2026-09-20 | **ANCHOR-SDF2 — full SDF bank<->anchor loop LIVE, both directions**: scenario `fiat/assetCode/deliveryMethod`, clearly-fake per-transaction test KYC (`ensureTransactionCustomer`), `onCustomerInfoRequired` polling + `incomplete` polls, deferred withdraw account resolved before paying; live deposit 10 USD→10 SRT + withdraw 10 SRT→USD both `completed`, check green, stellar 1044 tests green | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/anchor-sep10-validity.md` | P1 |
| 2026-09-20 | **NAV — voice navigation**: read-only `navigate` tool (tr/en synonyms + STT garbles, unknown→ask), additive `NavigationRequest` seam (`IntentKind` unchanged), shell opens notch pages / panel windows + spoken confirmation; agent 190, app 357, live `e2e:prompt` 54/54=100% | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/nav-voice-navigation.md` | P1 |
| 2026-09-20 | **RMTRAY — remove the menu-bar tray**: tray setup/menu/handlers + `tray-icon` feature removed (panels kept), notch "⋯" menu (`MoreMenu.tsx` + `MORE_MENU`/`quitPolaris` in `lib/panels.ts`) opens every former tray panel + new `quit_app` command; check/app 357/build/cargo 317/clippy green, no menu-bar icon + ⋯/Quit need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/rm-tray.md` | P0 |
| 2026-09-20 | **NW3 — Notch Tasks page → real scheduled payments**: `notch/data/useTasksData.ts` (mappers + thin hook, demo only outside Tauri/no owner), `TasksPage.tsx` (next local+UTC, amount/alias, recurrence, runs left, keeper hint, Cancel via `txPipeline`, Retry/empty states), 6 mapper tests; check/app 299/build green; live notch + Touch ID/Freighter need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/nw3-tasks-page.md` | P1 |
| 2026-09-20 | **MERGE-MAIN — merge Fatih's notch shell (origin/main #23/#24) into `integration/wallet`**: App.tsx shell/turn-session bridge, lib.rs union, debug-check port to `getShellGeometry`, `window_size` BUG-1 fix; typed prompt wired through `executeApprovedIntent`; app 292, agent 171, stellar 112, cargo 305 + clippy green | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/merge-main-notch-shell.md` | P0 |
| 2026-09-20 | **T1-demo — demo script, pitch, README status (docs only)**: `docs/demo-script.md` (3–4 min live script + fallbacks + pre-flight), `docs/pitch.md`, README status sections refreshed; live mic/Touch ID/Freighter marked `[verify live]` | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/t1-demo-script.md` | P0 |
| 2026-09-20 | **W8b — P2P ramp (client + voice + panel)**: `@polaris/stellar` `p2p` client (create/accept/confirm/cancel/reclaim + reads, mocks), `p2p_*` voice intents + tools, P2P panel via `txPipeline`, Debug check + `POLARIS_P2P_CONTRACT_ID` config field; live contract + real Mac unverified | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w8b-p2p-client.md` | P1 |
| 2026-09-20 | **W8b — P2P ramp (client + voice + panel)**: `@polaris/stellar` `p2p` client (create/accept/confirm/cancel/reclaim + reads, mocks), `p2p_*` voice intents + tools, P2P panel via `txPipeline`, Debug check + `POLARIS_P2P_CONTRACT_ID` config field; review fixes applied (union `OfferState` decode, both confirm+reclaim after deadline, paging, accept-card terms, live read) | opencode worker (deepseek-v4.1-flash) | open (review fixes applied) | `backlog/w8b-p2p-client.md` | P1 |
| 2026-09-20 | **W1 — network wiring**: `stellar_config` command + `StellarConfig`, build-then-approve execution seam (summary + `payloadHash`), lazy `chain.ts` config with alias merge, live `e2e:build-xdr` proof; signing/submission handed off | worker (deepseek-v4.1-flash) | open (review) | `backlog/w1-network-wiring.md` | P0 |
| 2026-09-20 | **W1-fix — review corrections**: `xdrDigest` vs Stellar tx hash disambiguation + cross-suite regression test, malformed tool result fails closed, `executeIntent` never-throw holes closed, StrKey CRC16 checksum, SHA-256 multi-block/UTF-8 vectors, stale-doc fixes | worker (deepseek-v4.1-flash) | open (review) | `backlog/w1-network-wiring.md` | P0 |
| 2026-09-19 | **A0 — harness**: global hotkey (press/release) + microphone capture to WAV + wire both into the existing log pane; delete the temporary `dev_self_test` command | Owner A | open | `backlog/2026-09-19-monorepo-skeleton.md` | P0 |
| 2026-09-19 | Owner B: fill in `stellar/` (anchor SEP-10/38/6 client, `sendPayment` tool) — stubs throw `NotImplementedError` today | Owner B | open | `backlog/2026-09-19-monorepo-skeleton.md` | P0 |
| 2026-09-19 | **TTS voice**: no professional Turkish voice on Fish Audio; decide spoken-output language. Also: expose a user-facing male/female voice choice (Sarah / Ethan) in a later milestone | Owner A | open | `backlog/2026-09-19-tts-voice-selection.md` | P1 |
| 2026-09-20 | **W0 — Interactive panel windows**: Rust panel registry + `open_panel` command, menu-bar tray (Wallet… / Settings… / Quit), `panel-*` capability, hash routing + panel skeletons; review corrections applied (§8 of the report), tray/window behaviour still needs a human on a real Mac | Owner A (worker) | open | `backlog/w0-panel-windows.md` | P0 |
| 2026-09-20 | **W0c — five more panel skeletons**: registered `security`/`schedules`/`suggestions`/`anchor`/`p2p` (Rust registry + routes + `PanelRoot` + skeletons) and reworked the tray to one item per panel; app check/tests/build + cargo test/clippy green, windows need a human Mac | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w0c-more-panels.md` | P0 |
| 2026-09-20 | **W3 — Touch ID approval gate**: independent-review fixes applied (WalletOnly webview bypass closed via in-process `begin_wallet_only`; W2 `{kind,message}` error contract + snapshot returns; panic-safe `in_flight` guard; F3–F7/F10/F11). Real Touch ID/password/cancel still need a human; **W5 must land the Rust anchor check before any WalletOnly release; W4b must not enable WalletOnly** | opencode-go/deepseek-v4.1-flash (L2, W3) | in review | `backlog/w3-touch-id-gate.md` | P0 |
| 2026-09-20 | **W4a — Freighter signing bridge page** (Stellar Wallets Kit): `/sign` second Vite entry, pure state machine, fail-closed address/network checks, signed-vs-unsigned hash binding, local fixture + tests, protocol docs; review fixes 2 applied | opencode worker (deepseek-v4.1-flash) | in review (fixes applied) | `backlog/w4a-freighter-bridge-page.md` | P0 |
| 2026-09-20 | **W4b-2 — wiring (TS)**: Touch ID approver, `bridge_sign`→`submitSignedTx`→`tx_submitted` path, enriched outcome + tr/en spoken result, Rust `tx_submitted_emit` command + validation, W4 Debug checks (`network`/`approval`/`bridge`/`submit`); needs W4b-1 Rust merged + a human to observe live | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w4b-wiring.md` | P0 |
| 2026-09-20 | **W4b-1 — Rust bridge server + independent signature verification**: one-shot loopback `tiny_http` session (one-time token, TTL, const-time compare, Host/Origin/CSP caps), `bridge_sign`/`bridge_selftest`/`bridge_health`, parse-free XDR verification + in-repo StrKey, browser launcher; real browser + Freighter round trip still needs a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w4b-bridge-server.md` | P0 |
| 2026-09-20 | **W0d — shared panel tx pipeline**: `app/src/lib/txPipeline.ts` (`runTx`/`runTxSequence`, injectable deps, never throws) + `useTxRun.ts` hook, reuse the Touch ID approver/`signAndSubmit`; fake-based tests; docs §10 | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w0d-tx-pipeline.md` | P1 |
| 2026-09-20 | **F2 — assistant system prompt**: `capabilities.ts` (role + registry-generated capability list + config account table + few-shots), `accountRefs.ts` normalisation, app config wiring, live `e2e:prompt` eval 31/31=100% | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/f2-assistant-prompt.md` | P0 |
| 2026-09-20 | **F3 — STT reliability**: bilingual Groq `prompt` (aliases from `POLARIS_ALIASES`), one-shot allowed-language retry (`POLARIS_STT_ALLOWED_LANGS`), short-audio hallucination filter, `GroqTransport` seam + `npm run stt:probe` live table; real-mic run still needs a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/f3-stt-vocab.md` | P0 |
| 2026-09-20 | **F1 — notch lifetime**: payment stages (`awaiting_approval`/`signing`/`submitting`) via additive `onStage`, collapse only from `done`/`error`, pending payment owns the notch (hotkey refused with a soft label), per-stage + 6 min total watchdogs, en/tr labels; 185 app tests pass | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/f1-notch-lifetime.md` | P0 |
| 2026-09-20 | **W5b — anchor flows + Anchor panel**: anchor `Signer` (seq-0 wallet-only `bridge_sign_challenge`, else Touch ID pipeline) + `createAnchorSession`, voice `deposit`/`withdraw` tools (`IntentKind` + Rust mirror), Anchor panel (SEP-1/38/6 flow, step list, explain log, forms), Debug check; live Touch ID/Freighter + panel windows need a human; depends on W5a | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w5b-anchor-flows.md` | P0 |
| 2026-09-20 | **W6a — Security & rules panel**: live on-chain state (rule/limits/executor/alias book/spent-today/allowance), Always-ask & Auto-under-limit profiles with validation + read-back, enable/tighten/disable via `runTxSequence` in the D13 safe order, alias editor, W6 Debug check; 12 pure tests | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w6a-security-panel.md` | P0 |
| 2026-09-20 | **W6b — scheduled payments**: voice intents `schedule_payment`/`cancel_schedule` (tr/en, explicit device zone), "Upcoming payments" panel (Cancel + New schedule + keeper strip) through `runTx`, `schedules` Debug check; live testnet demo + real Touch ID/Freighter need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w6b-schedules.md` | P1 |
| 2026-09-20 | **W6c — Wallet + Suggestions panels**: real Wallet content (owner/network/balances/alias book/last 10 payments, states + Refresh) from Horizon via `stellar_config`; Suggestions panel runs offline `suggest()` on owner history (Accept opens panel + copies draft, Dismiss in `localStorage`); `wallet`/`suggestions` Debug checks | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w6c-wallet-suggestions.md` | P1 |
| 2026-09-20 | **Merge-fix — integration/wallet**: repaired naive "keep both sides" damage in `interfaces/src/index.ts`, `agent/src/{index,prompt,runtime,tools/registry}.ts`, `app/src/lib/chain.ts`, `app/src-tauri/src/bridge/commands.rs`, `sprints.md`; all checks/tests/build/clippy green, probe resolves `acc2` | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/merge-fix-integration.md` | P0 |
| 2026-09-20 | **F4 — visible, specific failures**: Rust `polaris_log` command (terminal line, redactor, optional `error` event), TS `webLog` + `onerror`/`unhandledrejection` hooks and chain/signing/approver failure logs, specific refusal labels; redactor + label tests | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/f4-visible-errors.md` | P1 |
| 2026-09-20 | **W5a — anchor SEP-10 wallet-only signing (Rust)**: `bridge_sign_challenge` refuses any non-seq-0/owner-sourced/unsigned XDR, authorizes via in-process `begin_wallet_only`/`authorize_wallet_only`, verifies the challenge (same body, one added owner signature); seq-0/owner-source shape checks; parser hardened against crafted XDR (typed error, never a panic); generalised signature-list parsing (0–4 sigs); `anchor_signing_health`; real Freighter round trip still needs a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w5a-anchor-signing.md` | P0 |
| 2026-09-20 | **W9 — SPP in-app (read-only)**: SDK signer is pluggable but needs Freighter `signAuthEntry`, so shipped a read-only `privacy` panel (RPC status, contracts, verified spike evidence, explainer) + `lib/spp.ts` + Debug check; value-moving forms disabled; handoff in report | opencode-go/deepseek-v4.1-flash | open (review) | `backlog/w9-spp.md` | P2 |
| 2026-09-20 | **MERGE-FIX2 — post-W5a Rust test build**: fixed the two W5a-affected test initializers (`bridge/commands.rs:867/871`, added `challenge: false`); re-scanned the last three merges for keep-both damage (none found); cargo test 269/0/5-ignored, clippy `--all-targets -D warnings` clean, `npm run check` clean, app 268/0, agent 171/0 | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/merge-fix-integration.md` | P0 |
| 2026-09-20 | **MERGE-FIX3 — merge the reviewed W5a/W5b fix branches**: resolved `verify.rs` (panic-free parser + integration's `SourceMismatch`) and `chain.ts` (W6b schedules + W5b `runAnchorIntent` routing, F1/`onStage`/logging/Touch ID kept); fixed the `runTx` resequence seam the B1 review test exposed; check clean, app 301/0, agent 179/0, stellar 0 fail, build OK, cargo 310/0/5-ignored, clippy clean | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/merge-fix-integration.md` | P0 |
| 2026-09-20 | **W6a — Security & rules panel**: live on-chain state (rule/limits/executor/alias book/spent-today/allowance), Always-ask & Auto-under-limit profiles with validation + read-back, enable/tighten/disable via `runTxSequence` in the D13 safe order, alias editor, W6 Debug check; 11 pure tests | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w6a-security-panel.md` | P0 |
| 2026-09-20 | **W6a-fix — review corrections**: per-step resequencing in shared `runTxSequence` (B1, reuses `resequenceEnvelope`), mode-driven plan (M2), alias union + read-failed (M3), honest wording/amounts + in-flight double-click guard (N4–N8) | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w6a-security-panel.md` | P0 |
| 2026-09-20 | **Notch shell port onto `main`** + BUG-1 left-edge flash + BUG-2 voice animation. Port and both fixes are in; only the BUG-1 "after" screen recording is blocked (machine locked its screen mid-verification) | opencode worker (`opencode-go/deepseek-v4.1-flash`) | open (verification pending) | `backlog/2026-09-20-notch-shell-port.md` | P1 |
| 2026-09-20 | **Notch pages voice wiring**: call `setNotchPage(...)` (seam in `app/src/notch/notchPage.ts`) from Turkish intents + propose the `panel` state; then wire the 4 mock pages to real data | Owner A | open | `backlog/2026-09-20-notch-pages.md` | P1 |
| 2026-09-20 | **W8a — P2P escrow contract** `polaris_p2p_escrow`: built + 20 tests + testnet deploy `CBMXLTXS76S72SIPLVMCQOSS6SN2CR4V3Q73GZPRA4GRIBEM7RE5OLJW`; TS client + order-book UI pending | Worker (opencode) | open | `backlog/w8a-p2p-escrow.md` | P1 |
| 2026-09-20 | **T1-defaults — send defaults, balance tool, more assets**: `send_payment` no longer defaults a missing asset (asks instead), new read-only `get_balance` tool with deterministic spoken sentence + `toSpeech` loop branch (app injects a `stellar_config`/Horizon reader), PGUSD added to the chain asset registry; check + agent/stellar/app tests green, live eval 31/31=100% | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/t1-asset-defaults.md` | P1 |
| 2026-09-20 | **W5b-fix — review corrections**: real pipeline captures the signed XDR (+ fake-bridge tests), voice `deposit`/`withdraw` drive `runAnchorIntent` on the panel `AnchorSession` (seq-0 → `bridge_sign_challenge`, value → `txPipeline`), stricter missing-command check, directional step mapping, configured passphrase; W5a must reject non-seq-0 XDR | opencode worker (deepseek-v4.1-flash) | fixed (re-review) | `backlog/w5b-anchor-flows.md` | P0 |
| 2026-09-20 | **W4b2-fix — apply the W4b-2 review**: confirmed F1's per-stage watchdog (140 s approval) and added `shouldSurfaceOutcome` so a submitted tx is never dropped; lazy `@polaris/stellar` imports in `signing`/`submit`/`network`; `isBridgeSigned` payload validation; `tx_submitted_emit` doc + Wallet "reported by the app" label; approver deadline armed before `open`; MAJOR-2 eager-bundle goal blocked by out-of-scope W5/W6 modules | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w4b2-fix.md` | P0 |
| 2026-09-20 | **T1 — Settings / env / Makefile**: read-only Settings panel (`voice_health` + `stellar_config` rows, status badges, redacted Copy diagnostics) + `settings` Debug check; `.env` macOS app-support fallback; `make build` (local Tauri CLI) + `make run`; human-verify tray/clipboard/Finder launch | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/t1-settings-env.md` | P1 |
| 2026-09-20 | **NW1 — Notch Wallet page on real data**: `notch/data/useWalletData.ts` (stellar_config + Horizon + alias book + session `tx_submitted` latest tx, pure mapper) wired into `WalletPage.tsx`; check/tests 301 pass/build green; live notch needs a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/nw-wallet.md` | P1 |
| 2026-09-20 | **NW2 — Notch Rules page → real guard rules**: `RulesPage.tsx` is a read-only summary, `useRulesData.ts` (thin hook + pure `mapRulesView`) reuses the Security panel's `stateLines` read-back; "Edit rules" opens the Security panel; check/app tests 299/0/build green, live notch needs a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/nw-rules.md` | P1 |
| 2026-09-20 | **NW4 — notch History page → real data**: local 50-turn log (`lib/turnLog.ts`, fed from `App.tsx`) merged with owner Horizon payments via pure `historyModel.ts` mappers; `useHistoryData` with demo-only fallback + error/Retry, Refresh + "Clear local history"; `history` Debug check; check/app 308 pass/build green, real notch needs a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/nw-history.md` | P1 |
| 2026-09-20 | **F3-fix — prompt leak**: language-aware prompt (EN/TR only when forced), prompt-echo + forced-language-mismatch filters, 700 ms pre-API minimum, agent unintelligible-transcript rule; live probe shows no echo; real-mic run still needs a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/f3-stt-vocab.md` | P0 |
| 2026-09-20 | **F3-agent — unintelligible rule in the capabilities prompt**: `agent/src/capabilities.ts` gains the empty/garbled-transcript bullet + 120-char cap + no-tool few-shot, `prompt.test.ts` asserted against the composed prompt; eval +2 unintelligible cases (33/33 = 100%); check/agent 182/app 354 green | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/f3-stt-vocab.md` | P0 |
| 2026-09-20 | **W10b — Wallet login + recipients in the notch UI**: engine-detected onboarding (create phrase-shown-once / import preview→confirm), account list (select/rename/remove, balances, Friendbot), recipients "rumuz" (nickname + StrKey checksum), Rust `contacts.rs` store + `stellar_config` alias merge, per-turn alias refresh, no-wallet gate via NAV; app 382, agent 190, cargo 326/5-ignored, clippy clean; live notch/Touch ID need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w10b-wallet-ui.md` | P1 |
| 2026-09-20 | **HOVER — notch hover dead after a panel**: panels' `set_focus` activates the accessory app and macOS pauses the global mouse monitor while active; `lib.rs` now resigns active when the last panel closes; hover edge/state + `applied/target/source` logs, `notch_hover_health` + `notch_simulate_hover`, `notchHover.ts` Debug check, `shellVoiceInputs` + fail→settle→hover tests; check/app 356/build/cargo 319/clippy green, real notch unverified | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/fix-notch-hover.md` | P0 |
| 2026-09-20 | **BLOBATAR — Polaris gets a face**: `blobatar`/`@blobatar/react` 2.7.0, animated avatar posed from the voice stage. **Round 2:** the "motionless" face was measured, not guessed — the idle layer is authored in viewBox units, so at 22px its whole excursion was 0.24-0.48px; now 28px alone in the voice ear (dots removed) and 104px in the panel header, pointer gaze via `useGaze`, one element moved by CSS so the pose morph/idle phase/gaze survive the state change (verified by element identity). Review items folded in (entrance delay, `surprised` channel numbers, clipped halo, test-coverage claim). check.sh green, app 377/0, +9.4 KB gzip; **WKWebView visual still needs a human eye** | Claude Opus 5 (worker) + deepseek-v4.1-flash (round 3) | merged (PR #30, squash `b017a4a`) | `backlog/blobatar-avatar.md` | P1 |
| 2026-09-20 | **BLOBATAR-REVIEW — PR #30 review (CLAUDE.md §3)**: verified the remount claim against `render.d.ts` + the adapter source (the adapter memoises on `JSON.stringify([name, opts, animate])` and keeps `inner` byte-identical across poses, so the pose reaches the DOM as `style`/`className` and is diffed in place — the invariant holds, for a tighter reason than the PR gave), the collapsed gate, the `.notch-indicator` → `.notch-trail` handoff (no displacement on either axis), dependency hygiene (2 lockfile entries, no install hooks, no network/eval) and scope; re-ran check.sh green, app **372/0**, build green, and rebuilt the baseline to confirm **+6,994 B gzip** rather than trusting the author's "before" column. Outcome: **approve the code, 3 doc corrections requested** (wrong `esy` figure in 3 artifacts; "tests assert the tree" — there is no tree test and no infra for one; entrance comment promised an `animation-delay` the CSS lacked). All three were folded into round 2. Rounds 2-3 were merged on the owner's decision without a second review pass | Claude Opus 5 (reviewer) | done | `backlog/blobatar-avatar-review.md` | P1 |
| 2026-09-20 | **W10 — embedded wallet (Rust core + TS lib)**: `wallet/` (BIP-39 create, SEP-5 import/derive, macOS Keychain + 0600 fallback, zeroized seeds, `ed25519-dalek` signing reusing the bridge's parse-free verify), fixed command contract (`wallet_status/list/create/import_preview/import/select/rename/remove/sign/sign_challenge` + `wallet_changed`, typed `{kind,message}`), `stellar_config.signer` default `embedded`, TS `lib/wallet.ts` + signer routing in `signing.ts`/`anchor.ts`; cargo 340/0/5-ignored + clippy clean, app 357/0, agent 182/0, build green; Touch ID/Keychain/Friendbot need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w10-embedded-wallet.md` | P0 |
| 2026-09-20 | **MERGE-WALLET — reconcile the wallet engine (W10) with the wallet notch UI (W10b)**: one `wallet.ts` (W10 free wrappers + W10b `walletEngine`, wire types single-sourced in `@polaris/interfaces`), `stellar_config` keeps both the active-wallet `ownerAddress` override and the contacts alias merge; check clean, app 385/0, agent 190/0, stellar 112/0, build OK, cargo 349/0/5-ignored, clippy clean; Touch ID/Keychain/real notch need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w10b-wallet-ui.md` | P0 |
| 2026-09-20 | **W10-fix — apply the wallet security review**: Keychain is the only default store (plaintext file only with `POLARIS_WALLET_ALLOW_FILE_STORE=1`), per-account `store` metadata ends orphaned seeds, `wallet_sign` refuses an account changed after approval, owner override only when `embedded`, one signer resolver, Debug warns on non-Keychain, zeroize/0600/label/`CreateOutcome` Debug redaction NITs fixed; cargo 362/0/5-ignored + clippy, check/app 385/agent 190/build green; live Keychain/Touch ID need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w10-review-fixes.md` | P0 |
| 2026-09-20 | **W13a — wallet session (login / logout / auto-lock), Rust-enforced**: `wallet/session.rs` session state machine + `wallet_session`/`wallet_unlock`/`wallet_lock`/`wallet_set_auto_lock` + idle timer (injected clock), `wallet_sign`/`remove`/`select`/`rename` + approval path + `ownerAddress` gated by `Locked`, `wallet_session_changed` event, `walletSession.ts` Debug check; check green, app 385/0, cargo 362/0/5-ignored, clippy clean; real Touch ID/auto-lock/W13b UI need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w13a-wallet-session.md` | P1 |
| 2026-09-20 | **W11b — autonomous payments on (TS/UI)**: pure `autopay.ts` (D13 plan + defaults, fail-closed routing table incl. daily remaining/hard cap, #103–#107/#116 mapping, batch view) with tests, `autopayLive.ts` (injectable executor/batch commands + feature detection), `autopayWiring.ts` (`runGuardPolicySetup` + `tryAutoPaySend` via `executor_sign_pay`, records `approvalMode:"auto"`), chain.ts routing, batch approval card, functional Rules page, `autopay.ts` Debug check, live `npm run e2e:autopay` (pay_executor 1 XLM unattended SUCCESS, 15 XLM refused #105); check/app 441/agent 213/stellar green/build green; Rust W11a commands + Touch ID need a human | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/w11b-autopay-ui.md` | P0 |
| 2026-09-20 | **BLOBATAR — Polaris gets a face**: `blobatar`/`@blobatar/react` 2.7.0, animated avatar posed from the voice stage. **Round 2:** the "motionless" face was measured, not guessed — the idle layer is authored in viewBox units, so at 22px its whole excursion was 0.24-0.48px; now 28px alone in the voice ear (dots removed) and 104px in the panel header, pointer gaze via `useGaze`, one element moved by CSS so the pose morph/idle phase/gaze survive the state change (verified by element identity). Review items folded in (entrance delay, `surprised` channel numbers, clipped halo, test-coverage claim). check.sh green, app 377/0, +9.4 KB gzip; **WKWebView visual still needs a human eye** | Claude Opus 5 (worker) | open (re-review) | `backlog/blobatar-avatar.md` | P1 |
| 2026-09-20 | **ONBOARDING-native — first-run window + permission commands**: `app/src-tauri/src/onboarding.rs` (centered focusable 900x640 window, `onboarding_state/open/close/complete/reset/permissions/request_microphone/request_accessibility/open_settings`, `onboarding_permissions` event + visible-only 1s poll, marker `onboarding.json`), `onboarding.html` + placeholder entry + vite input, `capabilities/onboarding.json`, lib.rs startup open + shared panel/onboarding resign rule; check.sh green, cargo 328/0/5 + onboarding 8/8, clippy/tsc/build clean; React UI is a parallel worker, live window/TCC/AX need a human Mac | opencode worker (deepseek-v4.1-flash) | open (review) | `backlog/onboarding-native.md` | P0 |

## Completed Tasks (Archive)

| Date | Task | Worker/Agent | Closed By | Report |
|---|---|---|---|---|
| 2026-09-20 | **MERGE-PR38 — merge `integration/wallet-login` (PR #38) into `main`**: resolved the final 6 content conflicts (Cargo.toml deps union, lib.rs onboarding-only window handling, ShellSurface sfx + wallet-session integration, vite.config main+onboarding entries, backlog union, regenerated package-lock.json); `scripts/check.sh` + `--chain` green | W-1 DeepSeek (opencode-go/deepseek-v4.1-flash) | squash PR #39 → main (commit 2157504) | `backlog/pr38-merge-resolve.md` |
| 2026-09-20 | **REVIEW-PR39 — review PR #38 conflict resolution (PR #39)**: independent review of the conflict resolution; confirmed 6 content conflicts cleanly resolved and 7 deleted files absent; checks green; PR #39 approved | W-2 Gemini | squash PR #39 → main (commit 2157504) | `backlog/pr38-merge-resolve-review.md` |
| 2026-09-19 | Monorepo skeleton: `interfaces/`, `agent/`, `app/` (Tauri v2 + React), `stellar/`, `contracts/` (Soroban), tooling (`Makefile`, `scripts/`, `VERSION`, `CHANGELOG.md`) | coordinating agent (no workers, per user instruction) | pending PR review | `backlog/2026-09-19-monorepo-skeleton.md` |

---

## Sub-Report Template — `backlog/<task-name>.md`

```markdown
# Report: <task-name>
- **Date:** YYYY-MM-DD
- **Worker/Agent:** <name/model>
- **Branch/Worktree:** <branch>
- **PR:** <link or "none">

## Completed
- ...

## Unfinished (handed off)
- ...

## Blockers
- ...

## Review Notes
- ...

## Suggested Next Step
- ...
```

| 2026-09-20 | Dark Polaris product landing page; demo video integration pending | landing worker | review | `backlog/dark-landing.md` | P2 |
