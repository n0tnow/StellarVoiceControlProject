# Report: A3 — Text-to-speech (Fish Audio, with local fallback)

- **Date:** 2026-09-19
- **Worker/Agent:** opencode worker (deepseek-v4.1-flash)
- **Branch/Worktree:** `feat/a3-tts` / `.worktrees/a3` (based on `feat/a1-ondevice-stt`)
- **PR:** none (per the task: push only, no PR, no merge, no tag)

## Objective

Make Polaris speak: expose a `speak(text)` command so the assistant can read a
short confirmation or answer aloud. **Fish Audio** (`s2.1-pro-free`) is the
primary provider; because the free tier is time-limited and has **no SLA**, an
empty/invalid key or any network failure must fall back to **local macOS speech**
rather than failing the demo. Every utterance must use the **same voice**, which
for Fish Audio means sending the configured `reference_id` on every request.

## Completed

1. **A `Speaker` seam, mirroring A1.** New `app/src-tauri/src/tts/`:
   `tts::Speaker` is a one-method trait (`speak(&str) -> Result<(), TtsError>`
   plus a `name()` logging aid), with `fish.rs` (cloud) and `local.rs`
   (on-device) behind it and `build_backend()` selecting between them. This is
   the same shape as `stt::Transcriber`; only the direction is inverted.
2. **Fish Audio backend.** `POST https://api.fish.audio/v1/tts` with
   `Authorization: Bearer $FISH_AUDIO_API_KEY`, header `model: s2.1-pro-free`,
   and JSON body `{text, reference_id, format, latency}`. The body is built by
   serde from a small struct via the pure `fish::request_body`, and a unit test
   pins that `reference_id` is present in the serialised bytes. The key only ever
   appears in the `Authorization` header; it is never logged, written or included
   in an error.
3. **One fixed voice — enforced.** `POLARIS_TTS_REFERENCE_ID` is **required** for
   the Fish backend. If it is missing or blank, `FishSpeaker::missing_config()`
   reports it before any request is made; `build_backend()` logs the reason and
   constructs the local backend instead of sending a voice-drifting request. The
   check runs **before** the key check, because a request without the voice id is
   exactly what the owner forbids.
4. **Mandatory local fallback.** `FallbackSpeaker` wraps the Fish primary and the
   local `say` backend and falls back on **any** primary failure (missing key,
   network, HTTP, malformed audio) — deliberately broader than A1's narrow
   `Unavailable`-only fallback, because the A3 requirement is that the assistant
   is never silent. Each fallback prints why and notes that this utterance uses a
   different voice. A local-only build is used when Fish config is absent.
5. **Selection.** `POLARIS_TTS_BACKEND` (`fish` | `local`, case-insensitive),
   default `fish`; an unknown value resolves to `fish` **and** reports
   "unrecognized" so the caller warns (`tts::parse_backend`, unit-tested).
6. **Playback.** Fish returns audio bytes (MP3 by default). The simplest approach
   that actually plays them from Rust is used: write to a unique temp file under
   the OS temp dir and hand it to macOS's built-in `afplay`, which supports MP3
   and WAV natively. No new dependency. The local backend needs no separate
   playback step: `say` synthesizes and plays in one call.
7. **Command + events + latency.** `commands::speak` returns a typed
   `SpeechOutcome {backend, latencyMs, characters}` on success and
   `SpeechFailure {label, detail}` on failure. Success prints
   `polaris: tts in <ms> ms via <backend> (<n> chars)`; failure prints the full
   detail to the terminal and emits the **existing** `error` event with a short
   `TTS: <label>` message. No new `PolarisEvent` variant was added: adding one
   would require editing the TS seam (`interfaces/src`, `app/src/lib`), which is
   worker A2's scope. Playback runs on Tauri's blocking pool
   (`spawn_blocking`), never the main thread.
8. **Turkish and English.** Text is passed through unchanged and serialised with
   serde; a test covers a Turkish string with punctuation.

## Env vars introduced (for the coordinator to merge into `.env.example`)

`.env.example` is worker A2's file, so it was **not** touched. Variables read by
this module:

| Variable | Default | Meaning |
|---|---|---|
| `POLARIS_TTS_BACKEND` | `fish` | `fish` or `local`; unknown warns and uses `fish` |
| `FISH_AUDIO_API_KEY` | (none) | Fish Audio bearer token; required for `fish` |
| `POLARIS_TTS_REFERENCE_ID` | (none) | Fish voice-model id; **required** for `fish` (voice stability) |
| `POLARIS_TTS_MODEL` | `s2.1-pro-free` | Fish engine id sent in the `model` header |
| `POLARIS_TTS_FORMAT` | `mp3` | Output container (`mp3`, `wav`, `pcm`, `opus`) |
| `POLARIS_TTS_LATENCY` | `normal` | Fish latency/quality trade-off |
| `POLARIS_TTS_LOCAL_VOICE` | `Yelda` | Local macOS `say` voice |

`FISH_AUDIO_API_KEY` and `POLARIS_TTS_REFERENCE_ID` already exist in the
worktree's gitignored `.env`. **Note:** the brief said the worktree `.env` has
`FISH_AUDIO_API_KEY=` empty, but the file on disk currently contains a non-empty
value. Because the brief states the key is not considered provisioned, I did
**not** make a live Fish request (see "Unverified" below); the live call remains
the documented manual check.

## Fish Audio API facts confirmed before writing the client

Read from <https://docs.fish.audio/api-reference/endpoint/openapi-v1/text-to-speech>
and <https://docs.fish.audio/llms.txt> while implementing:

- Endpoint `POST https://api.fish.audio/v1/tts`, auth `Authorization: Bearer <token>`.
- `model` is a **header** (not body): `s1`, `s2-pro`, `s2.1-pro`,
  `s2.1-pro-free`, `drama-3-preview`; omitted/unknown falls back to `s2.1-pro`.
  `s2.1-pro-free` is the free developer tier.
- Body is `application/json` (or `application/msgpack`); `text` is required.
  `reference_id` (string) selects the **voice**; the docs' own single-speaker
  cURL sample includes `"format": "mp3"` and `"latency": "normal"`.
- `format` is `wav` | `pcm` | `mp3` | `opus`; MP3 defaults to 128 kbps, 44.1 kHz,
  mono. Response is the raw audio (`audio/mpeg` for mp3).

## Files changed

- `app/src-tauri/src/tts.rs` (new — trait, errors, fallback, selection, `speak_and_log`)
- `app/src-tauri/src/tts/fish.rs` (new — Fish Audio HTTP client + request body)
- `app/src-tauri/src/tts/local.rs` (new — macOS `say` backend)
- `app/src-tauri/src/tts/player.rs` (new — temp-file write + `afplay`)
- `app/src-tauri/src/commands.rs` (`speak` command + `SpeechOutcome`/`SpeechFailure`)
- `app/src-tauri/src/lib.rs` (register `mod tts`, manage the backend, add the command)
- `docs/architecture.md` (§4.1 TTS row, §7 status, diagram node)
- `backlog/2026-09-19-a3-tts.md`, `backlog.md`, `notes.md`
- **Not touched:** `.env`, `.env.example`, `agent/**`, `interfaces/**`, `app/src/**`,
  `stellar/**`, `contracts/**`, `app/src-tauri/src/stt/**`, `TASK-A3.md`.

## Commands run and their REAL output

All long commands were wrapped in `caffeinate -i`, run from `app/src-tauri`.

**Baseline, before any change:**
```
$ caffeinate -i cargo test
...
test result: ok. 69 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
```
(The task brief quoted 57; the `feat/a1-ondevice-stt` branch already carries 69.)

**Build:**
```
$ caffeinate -i cargo build
   Compiling polaris-app v0.1.0 (/Users/fatih/StellarVoiceControlProject/.worktrees/a3/app/src-tauri)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 2.87s
```
No warnings.

**Tests (no network; fake backend):**
```
$ caffeinate -i cargo test
test result: ok. 96 passed; 0 failed; 1 ignored; 0 measured; 0 filtered out
test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
```
96 total = 69 baseline + 27 new. The single ignored test is the manual,
audio-producing fallback demo (below); it is excluded from the default suite.

**Clippy (all targets):**
```
$ caffeinate -i cargo clippy --all-targets
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 5.81s
```
0 warnings, 0 errors.

**Manual fallback demonstration — empty Fish key, local speech.** Run with the
`#[ignore]`d test, which drives the exact `build_backend()` + `speak_and_log()`
path the `speak` command uses:
```
$ caffeinate -i cargo test tts::tests::manual_local_fallback_speaks_when_fish_key_is_missing -- --ignored --nocapture --exact

running 1 test
polaris: POLARIS_TTS_BACKEND=fish but FISH_AUDIO_API_KEY is not set — speaking with local macOS speech instead
polaris: tts in 4659 ms via local (35 chars)
test tts::tests::manual_local_fallback_speaks_when_fish_key_is_missing ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 4.66s
```
The first line is the required "why it fell back" message; the audio was actually
spoken by `say` (`Yelda`, Turkish). The same test asserts `backend.name() == "local"`.

**TypeScript:** not run. No file under `app/src/**` or `interfaces/**` was
changed, so there is nothing for `npm run typecheck` / `npm run build` to cover on
this branch (and A2 owns those files).

## Manual check to run once a real key is provisioned

1. Ensure `.env` at the repo root has a valid `FISH_AUDIO_API_KEY` and
   `POLARIS_TTS_REFERENCE_ID`, and `POLARIS_TTS_BACKEND=fish`.
2. Launch the shell (`npm run tauri:dev` from `app/`), then from the webview call
   `invoke("speak", { text: "Polaris hazır. Sistemi test ediyorum." })`, or call
   the same path directly:
   ```
   caffeinate -i cargo test tts::tests::manual_local_fallback_speaks_when_fish_key_is_missing \
     -- --ignored --nocapture --exact
   ```
   only with the key present the log should read `tts in <ms> ms via fish` and the
   audio should be the configured voice; force a failure (temporarily wrong key)
   to see the fallback line. The coordinator's 2026-09-19 `curl` already confirmed
   the provider contract (HTTP 200, `audio/mpeg`).

## Unverified

- **The live Fish path.** No request was sent, per the brief's statement that the
  key is not provisioned. The endpoint, headers and body match the provider docs
  and the coordinator's `curl`, but real synthesis, the returned byte stream,
  `afplay` of the real MP3, and the free tier's behavior are all untested here.
- Real playback latency for the Fish path (the local path measured ~4.7 s for a
  short sentence, dominated by `say`).
- A stable `reference_id` that actually sounds right in Turkish — that is a
  product/voice choice for the owner.

## Unfinished (handed off)

- A dedicated `speech_status` event (analogous to `capture_status`) was **not**
  added, to avoid touching the TS seam owned by A2. If the UI should reflect
  "speaking", A2 can extend `PolarisEvent` and wire the existing `speak` command.
- The agent loop (A2) does not call `speak` yet; that is the integration point.
- No mute toggle (was mentioned in the A3 sprint note); out of this task's scope.
- `sprints.md` A3 checkboxes were left for the coordinator to tick.

## Review Notes

- Scope respected: only `tts/**`, `lib.rs`, `commands.rs`, `docs/architecture.md`
  and the docs indexes changed. `events.rs`/`types.rs` were not needed because the
  existing `error` variant carries the failure label.
- `cargo fmt` is **not** clean repo-wide (pre-existing in `capture.rs`, `build.rs`),
  so rustfmt was applied only to the files this task touched to avoid unrelated
  churn. `cargo fmt --check` now reports no diff in any A3 file.
- Security: the key is read once from the environment, used only in the
  `Authorization` header, and absent from all log/error paths. `Command` args for
  `say` use `--` so text cannot inject flags.

## Suggested Next Step

Provision `FISH_AUDIO_API_KEY`, run the manual check above, and record the Fish
latency/voice result here. Then have A2 call `speak` from the agent loop and
decide whether a `speech_status` event is wanted.
